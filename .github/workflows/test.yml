name: Test API

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
    - name: Install dependencies
      run: npm install
    - name: Run tests (Unit/Lint)
      run: |
        # In a CI environment where a production DB is not connected,
        # we can still run syntax and basic jest checks (the tests will gracefully skip)
        npm test
